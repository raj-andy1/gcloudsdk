<% if @is_install_kubectl != false %>
  '<%= @install_path %>'/bin/gcloud components install kubectl -q
<% end %>

<% if @is_install_app_engine_python != false %>
  '<%= @install_path %>'/bin/gcloud components install app-engine-python -q
<% end %>

<% if @is_install_app_engine_java != false %>
  '<%= @install_path %>'/bin/gcloud components install app-engine-java -q
<% end %>

<% if @is_install_beta != false %>
  '<%= @install_path %>'/bin/gcloud components install beta -q
<% end %>

<% if @is_install_alpha != false %>
  '<%= @install_path %>'/bin/gcloud components install alpha -q
<% end %>

<% if @is_install_pubsub_emulator != false %>
  '<%= @install_path %>'/bin/gcloud components install pubsub-emulator -q
<% end %>

<% if @is_install_gcd_emulator != false %>
  '<%= @install_path %>'/bin/gcloud components install gcd-emulator -q
<% end %>