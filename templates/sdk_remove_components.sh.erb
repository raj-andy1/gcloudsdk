<% if @is_install_gsutil != true %>
  '<%= @install_path %>'/bin/gcloud components remove gsutil -q
<% end %>

<% if @is_install_core != true %>
  '<%= @install_path %>'/bin/gcloud components remove core -q
<% end %>

<% if @is_install_bq != true %>
  '<%= @install_path %>'/bin/gcloud components remove bq -q
<% end %>      
        
<% if @is_install_gcloud != true %>
  '<%= @install_path %>'/bin/gcloud components remove gcloud -q
<% end %>

